#include "zonemaps.h"

template <typename T>
zonemap<T>::zonemap(std::vector<T> _elements, unsigned int _num_elements_per_zone) {
  // constructor
}

template <typename T>
bool zonemap<T>::query(T key) {
  return false;
}
